type Query {
    getDemos: [ demo! ]
}

type Mutation {
    addDemo(input: DemoInput!): demo
}

type demo {
    id: String!
    version: String!
    currentDateTime: String!
}

input DemoInput {
    version: String!
}
